import React from 'react';
import { View, Text } from 'react-native'

const Logout = () => {
    return (
        <View>
            <Text>ini Logout</Text>
        </View>
    )
}
export default Logout